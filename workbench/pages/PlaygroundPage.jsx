import './PlaygroundPage.css';

/**
 * PlaygroundPage — freeform component playground.
 */
export function PlaygroundPage() {
  return (
    <div className="playground-page">
      <p className="playground-page__empty">Playground is empty. Add components to get started.</p>
    </div>
  );
}
